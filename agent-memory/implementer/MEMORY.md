# Implementer Memory (global)

Last curated: 2026-06-03 (seed)
Scope: cross-project execution craft, shared by `implementer-sonnet` and `implementer-haiku`. Read at the start of implementer work. Hand-curated by the architect; keep it short. Repo-specific build/test facts belong in that repo's `.agent-memory/roles/implementer/MEMORY.md`.

## Durable Learnings
- Touch only inventory files; the out-of-scope list is a hard deny-list. Unlisted file needed → halt and report.
- Re-read each touched region after every edit; anchor search/replace patterns.
- Never `git push`, never self-merge, don't commit unless asked. No destructive git, no migration downgrades.
- Halt on the first FAIL; report verbatim. Never amend the contract mid-run — record the bug for the architect.

## Topic Files
- None yet.

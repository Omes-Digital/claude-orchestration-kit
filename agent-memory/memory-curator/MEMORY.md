# Memory-Curator Memory (global)

Last curated: 2026-06-03 (seed)
Scope: how to curate this memory layer well. Read before promoting proposals or consolidating memory. Hand-curated by the architect; keep it short.

## Durable Learnings
- Promote only memory that is durable (changes future behavior), evidence-backed, smallest-correct-scope, and concise.
- Mark stale/superseded entries instead of deleting them outright.
- Re-read the target file before editing; keep each MEMORY.md short (context rot is real) — move detail to topic files.
- Reject vague, secret, customer-specific, one-off, or unsupported entries.
- **Built-in subagents (`Explore`, `Plan`, `general-purpose`) cannot self-read role memory** — the architect must inject the relevant `MEMORY.md` into their spawn prompt. Only agent defs/skills we own (implementer-*, review-diff) self-read.

## Rejected Patterns
- None yet.

## Supersession Notes
- None yet.
